# v1/v2 历史实现归档

本目录仅保存早期 snapshot、外部 WAL、writer 原型、迁移实验及其测试，便于追溯设计演进。

归档内容具有以下约束：

- 不参与默认 CMake 构建、安装、测试或 qualification。
- 不被当前运行库、格式检查工具或公共头文件引用。
- 不提供兼容性、安全更新或生产支持。
- 文件名、接口和文档保持归档时的历史语义，不与当前实现同步重构。

当前引擎只支持仓库根目录文档描述的单文件格式。若以后确实需要恢复历史数据，应把需求作为独立迁移项目重新设计和验证，不要把本目录直接链接回运行时。
